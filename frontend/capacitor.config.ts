import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eventfy.app',
  appName: 'Eventfy',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
